version: '3.8'
services:
  react-builder:
    container_name: react-component-builder
    build: .
    ports:
      - "1001:1001"
    volumes:
      - ./deployments:/app/deployments
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G